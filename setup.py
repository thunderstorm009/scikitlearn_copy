from setuptools import setup, find_packages

setup(
    name="scikitlearn_copy",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "numpy",
    ],
    author="Abrar",
    description="A simple from-scratch implementation of scikit-learn architecture.",
)
